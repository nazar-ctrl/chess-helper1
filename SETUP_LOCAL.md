# 🚀 Инструкция по локальному запуску

## Требования

- Браузер: Chrome, Firefox, Safari, Edge (любой современный)
- ОС: Windows, macOS, Linux
- Git (необязательно)

## Способ 1: Прямое открытие (самый простой)

### Windows
1. Откройте папку `c:\Users\user\Desktop\assist`
2. Найдите файл `index.html`
3. Двойной клик на `index.html`
4. Откроется в браузере

### macOS / Linux
```bash
# Откройте файл в браузере
open index.html
# или
xdg-open index.html
```

## Способ 2: Python сервер (рекомендуется)

### Требования
- Python 3 (или Python 2)

### Запуск

#### Windows (PowerShell)
```powershell
cd c:\Users\user\Desktop\assist
python -m http.server 8000
```

#### macOS / Linux
```bash
cd ~/Desktop/assist
python3 -m http.server 8000
```

### Использование
1. Откройте браузер
2. Введите URL: `http://localhost:8000`
3. Нажмите Enter

### Остановка сервера
```
Нажмите Ctrl+C в терминале
```

## Способ 3: VS Code с Live Server

### Требования
- VS Code
- Расширение "Live Server" (установить из маркетплейса)

### Запуск
1. Откройте папку проекта в VS Code
2. Кликните правой кнопкой на `index.html`
3. Выберите "Open with Live Server"
4. Откроется в браузере с автоматическим обновлением

## Способ 4: Node.js с npm

### Требования
- Node.js 14+
- npm

### Запуск
```bash
cd c:\Users\user\Desktop\assist

# Способ 1: http-server
npx http-server

# Способ 2: simple-http-server
npx simple-http-server
```

Откройте браузер: `http://localhost:8000`

## Способ 5: Docker (для опытных)

### Требования
- Docker установлен

### Запуск
```dockerfile
# Создайте Dockerfile в папке проекта:
FROM nginx:latest
COPY . /usr/share/nginx/html
EXPOSE 80
```

Запуск:
```bash
docker build -t chess-helper .
docker run -p 8000:80 chess-helper
```

Откройте: `http://localhost:8000`

## Проверка работоспособности

### 1. Откройте консоль браузера (F12)
- Не должно быть красных ошибок
- Может быть информация (белые сообщения)

### 2. Создайте аккаунт
- Имя: Тест
- Email: test@test.com
- Пароль: 123456

### 3. Подтвердите email
- В консоли браузера будет код
- Скопируйте и вставьте в форму

### 4. Начните игру
- Нажмите "Новая партия"
- Сделайте ход
- Проверьте, что ИИ отвечает

### ✅ Если всё это работает - приложение готово!

## Устранение неполадок

### "ОШИБКА: Файл не найден"
**Решение**: Убедитесь, что вы в правильной папке
```bash
# Проверьте путь
ls c:\Users\user\Desktop\assist\index.html
```

### "ОШИБКА: Порт 8000 уже занят"
**Решение**: Используйте другой порт
```bash
python -m http.server 9000
# Откройте http://localhost:9000
```

### "ОШИБКА: Python не установлен"
**Решение**: Установите Python с https://python.org
```bash
# Проверка установки
python --version
```

### "localStorage не работает"
**Решение**: Откройте в серьёзном браузере (не IE)
- Проверьте, включены ли cookies

### "Не видно друзей/приглашений"
**Решение**: Откройте F12 → Console
```javascript
// Проверьте localStorage
JSON.parse(localStorage.users)
JSON.parse(localStorage.gameInvitations)
```

## Быстрые команды

### Windows
```powershell
# Перейти в папку
cd c:\Users\user\Desktop\assist

# Запустить сервер
python -m http.server 8000

# Открыть в браузере
start http://localhost:8000
```

### macOS / Linux
```bash
# Перейти в папку
cd ~/Desktop/assist

# Запустить сервер
python3 -m http.server 8000

# Открыть в браузере
open http://localhost:8000
```

## Тестирование на разных браузерах

### Chrome
```
✅ Полная поддержка
```

### Firefox
```
✅ Полная поддержка
```

### Safari
```
✅ Полная поддержка
```

### Edge
```
✅ Полная поддержка
```

### Internet Explorer
```
❌ НЕ поддерживается (используйте Edge)
```

## Рекомендуемые браузеры

1. **Chrome** - самый быстрый, лучше всего работает
2. **Firefox** - хорошая поддержка, приватность
3. **Safari** - для Mac/iOS
4. **Edge** - хороший выбор, интеграция с Windows

## Отладка

### Консоль браузера (F12)

#### Windows
```
Chrome: F12 или Ctrl+Shift+I
Firefox: F12 или Ctrl+Shift+I
Edge: F12 или Ctrl+Shift+I
```

#### macOS
```
Chrome: Cmd+Option+I
Firefox: Cmd+Option+I
Safari: Cmd+Option+I
```

### Полезные команды в консоли

```javascript
// Посмотреть пользователей
console.log(JSON.parse(localStorage.users))

// Посмотреть текущего пользователя
console.log(JSON.parse(localStorage.currentUser))

// Посмотреть приглашения
console.log(JSON.parse(localStorage.gameInvitations))

// Очистить localStorage (внимание!)
localStorage.clear()

// Перезагрузить страницу
location.reload()
```

## Развитие на локальной машине

### Как редактировать файлы

1. Откройте VS Code
2. Откройте папку `c:\Users\user\Desktop\assist`
3. Редактируйте файлы
4. Сохраняйте (Ctrl+S)
5. Обновите браузер (F5)

### Если используете Live Server
- Файлы обновляются автоматически
- Не нужно обновлять браузер

## Коммит в Git

После тестирования и разработки:

```bash
# Проверить статус
git status

# Добавить файлы
git add .

# Коммитить
git commit -m "Описание изменений"

# Загрузить на GitHub
git push
```

## ✅ Готово!

Теперь вы можете:
- ✅ Запустить приложение локально
- ✅ Тестировать функции
- ✅ Разрабатывать новое
- ✅ Дебажить в консоли

Спасибо и приятной разработки! 🎉

