# Вклад в проект Chess Helper

Спасибо интересом к нашему проекту! Вот как вы можете помочь:

## Типы вклада

### 🐛 Сообщение об ошибках

Если вы нашли ошибку:

1. **Откройте Issue** на GitHub
2. **Описание**: Четко опишите проблему
3. **Шаги воспроизведения**: Как повторить ошибку?
4. **Ожидаемый результат**: Что должно происходить
5. **Скриншоты**: Если применимо

### ✨ Новые функции

Если у вас есть идея:

1. **Откройте Discussion** (или Issue с меткой "enhancement")
2. **Описание**: Подробно объясните идею
3. **Пример использования**: Как это будет работать?
4. **Альтернативы**: Есть ли другие способы?

### 📝 Улучшение документации

- Исправляйте опечатки
- Добавляйте примеры
- Улучшайте пояснения

### 🔧 Исправление кода

## Процесс разработки

### 1. Форкните репозиторий

```bash
# Откройте https://github.com/YOUR_USERNAME/chess-helper
# Нажмите кнопку "Fork"
```

### 2. Клонируйте свой форк

```bash
git clone https://github.com/YOUR_USERNAME/chess-helper.git
cd chess-helper
```

### 3. Создайте ветку для вашей функции

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/bug-name
```

### 4. Внесите изменения

- Следуйте стилю кода проекта
- Добавляйте комментарии где нужно
- Тестируйте свои изменения

### 5. Коммитьте изменения

```bash
git add .
git commit -m "Description of your changes"
# Примеры:
# "Add friend invitation system"
# "Fix pawn capture bug"
# "Improve documentation"
```

### 6. Загрузите в ваш форк

```bash
git push origin feature/your-feature-name
```

### 7. Откройте Pull Request

1. Откройте https://github.com/YOUR_USERNAME/chess-helper
2. Нажмите "Pull requests"
3. Нажмите "New pull request"
4. Выберите вашу ветку
5. Заполните описание PR
6. Нажмите "Create pull request"

## Руководство по стилю кода

### JavaScript

```javascript
// ✅ Хорошо
function makeMove(fromRow, fromCol, toRow, toCol) {
    if (!this.isValidMove(fromRow, fromCol, toRow, toCol)) {
        return false;
    }
    // ...
}

// ❌ Плохо
function makeMove(a,b,c,d){if(!this.isValidMove(a,b,c,d))return false;...}
```

### Комментарии

```javascript
// Объясняйте ПОЧЕМУ, а не ЧТО
// ✅ Хорошо
if (piece.type === 'pawn' && target.color !== color) {
    // Пешка может захватить только вражескую фигуру диагонально
}

// ❌ Плохо
// Проверка пешки
if (piece.type === 'pawn') {
```

### Именование

```javascript
// ✅ Хорошо
const isValidMove = true;
const capturedPieces = [];
function recordPawnCapture() { }

// ❌ Плохо
const valid = true;
const arr = [];
function rpc() { }
```

## Требования к коду

Перед отправкой PR убедитесь:

- [ ] Код работает без ошибок
- [ ] Нет консольных ошибок (F12)
- [ ] Следуете стилю проекта
- [ ] Добавили комментарии где нужно
- [ ] Протестировали в разных браузерах
- [ ] Обновили документацию если нужно

## Тестирование

### Перед отправкой PR

1. Откройте index.html в браузере
2. Протестируйте вашу функцию
3. Проверьте, не сломалось ли что-то еще
4. Откройте консоль (F12) - ошибок быть не должно

### Что тестировать

- Основная функциональность
- Граничные случаи
- Мобильная адаптивность (если применимо)
- Разные браузеры (Chrome, Firefox, Safari)

## Документация

### Если вы добавляете функцию

1. Добавьте комментарии в коде
2. Обновите FEATURES.md
3. Обновите README.md если нужно
4. Обновите CHANGELOG.md

### Пример документации

```markdown
### 🆕 Новая функция

**Описание:** Что это делает?

**Использование:**
\`\`\`javascript
yourFunction(param1, param2)
\`\`\`

**Параметры:**
- `param1` - описание
- `param2` - описание

**Возвращает:** Что возвращает функция?
```

## Процесс ревью

### Чего ожидать

1. **Автоматические проверки** - код проверяется
2. **Ревью от мейнтейнеров** - могут быть комментарии
3. **Запросы на изменения** - если нужны доработки
4. **Слияние** - когда все готово!

### Типичные комментарии

- Стиль кода не соответствует проекту
- Нужны дополнительные тесты
- Нужна документация
- Потенциальная проблема безопасности/производительности

## Рекомендуемые улучшения

Нам нужна помощь с:

1. **Backend** - создать Node.js сервер
2. **WebSocket** - реальная синхронизация
3. **База данных** - хранение на сервере
4. **Мобильное приложение** - React Native версия
5. **Тесты** - unit и интеграционные тесты
6. **Документация** - видеоуроки

## Вопросы?

- **Откройте Discussion** на GitHub
- **Прочитайте FAQ** в TESTING.md
- **Посмотрите примеры** в коде

## Кодекс поведения

Мы обязуемся создать безопасное и уважительное пространство.

Поведение, которое мы не терпим:
- Оскорбления или унижение
- Домогательства
- Дискриминация
- Спам

## Лицензия

Ваш вклад попадает под MIT лицензию (как весь проект).

---

**Спасибо за вклад!** 🎉

Каждый коммит делает проект лучше!

